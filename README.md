# cnote

Personal learning notebook. The published HTML is encrypted in the browser and marked `noindex`.

Before publishing, validate that the generated encrypted payload is complete:

```powershell
node scripts/validate-built-site.mjs
```
